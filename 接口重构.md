# VisionAIFlowV1 模型接口重构执行计划

更新日期: 2026-08-04

用途: 交给后续 Codex 任务执行模型接口重构. 本计划只描述重构步骤和验收标准, 不代表当前仓库已经满足重构前置条件.

## 1. 本次仓库检查结论

### 1.1 已确认完成的迁移内容

- 根工程 `VisionAIFlowV1.pro` 已从扁平子工程列表改为 `core`, `data`, `models`, `ai_engine`, `runtime`, `apps`, `tests` 七个分组工程.
- 新增的分组入口包括:
  - `cpp/core/core.pro`
  - `cpp/data/data.pro`
  - `cpp/models/models.pro`
  - `cpp/ai_engine/ai_engine.pro`
  - `cpp/runtime/runtime.pro`
  - `cpp/apps/apps.pro`
  - `tests/tests.pro`
- qmake, CMake 和构建脚本中的主要源码路径已切换到新目录.
- 对迁移前后 101 个 `.cpp`, `.h`, `.cu`, `.ui` 文件进行了 Git blob 字节比较. 结果为 101 个完全一致, 0 个缺失, 0 个内容变化. 当前源码迁移可以判断为纯路径迁移.
- 对当前源码和工程文件搜索后, 未发现仍引用旧扁平源码路径的有效代码.
- `git diff --check` 未报告已跟踪文件的空白错误.

### 1.2 重构前必须解决的问题

| 优先级 | 问题 | 影响 | 处理要求 |
| --- | --- | --- | --- |
| P0 | 当前工作区未提交. Git 将迁移显示为大量旧文件删除和新目录未跟踪, 不是已确认的 rename | 接口重构后将无法清晰区分目录迁移和语义改动 | 先完整暂存并复核 rename, 单独提交目录迁移基线, 再开始接口重构 |
| P0 | 工具链来源不一致 | 无法形成可信的 Release 基线, 还可能混用不同 MSVC ABI | 必须由用户确认唯一工具链, 并同步 `AGENTS.md`, `.qmake.cache`, `qmake/*.pri`, 构建脚本和文档 |
| P0 | 当前任务约定是 Qt 6.9.2 + MSVC2022 v143 19.44, 但仓库配置和 2026-08-04 构建日志使用 Qt 6.7.3 + MSVC2019 19.29 | 与项目约定冲突 | 未确认前禁止继续接口重构和发布构建 |
| P0 | 现有 Release 日志在 `ai_engine/training` 的 MOC 命令处结束, 没有完成整个工程 | 迁移后的全量编译没有通过证据 | 修正工具链后重新生成 qmake Makefile, 完成全量 Release 编译 |
| P0 | 构建日志中编译器来自 VS2019, 但 MOC include 列表又出现 `F:/VS2022/BuildTools` | 同一次构建存在工具链污染风险 | 清理构建环境变量和旧 Makefile, 确保 qmake, cl, link, nmake, Windows SDK 全部来自同一 Kit |
| P0 | `out/qmake/Release/bin` 中 15 个测试程序和 5 个应用程序均为 2026-07-22 的旧产物 | 旧程序不能证明 2026-08-04 的迁移结果可用 | 全量重编后再运行测试, 验收时校验产物时间和日志 |
| P1 | `cpp/tests/CMakeLists.txt` 是旧目录残留文件 | CMake 目录语义不一致 | 确认新 CMake 测试入口完整后删除或迁移. 删除前遵守仓库删除确认规则 |
| P1 | `重划目录.txt` 当前显示为已删除 | 迁移设计依据可能丢失 | 由用户决定归档, 恢复, 或由正式架构文档替代, 不要默认删除 |
| P1 | 存在较多空目录和旧目录骨架, 如 `cpp/application`, `cpp/dataset`, `cpp/inference`, `cpp/foundation`, `cpp/annotation`, `cpp/tests` | 容易误导后续开发者, 也会诱导提前创建空模块 | 仅保留已有真实实现或下一阶段立即使用的目录. 批量删除前必须让用户确认 |
| P1 | `qmake/common.pri` 仍向所有目标全局暴露几乎全部内部 include 路径 | 模块边界虽然在目录上形成, 编译依赖仍可随意穿透 | 接口重构稳定后按目标收紧 include 和 link 依赖, 不在第一步同时修改 |

### 1.3 当前模型代码的主要耦合点

- `cpp/ai_engine/training/include/visionaiflow/training/LinearClassifierTrainer.h` 同时包含网络定义, 训练和评估函数, 参数命名及 checkpoint 接口.
- `cpp/ai_engine/training/include/visionaiflow/training/Yolo11DetectionTraining.h` 同时包含两个 YOLO11 网络, target assignment, loss, metrics, augmentation, train/evaluate 和 checkpoint 接口.
- `cpp/ai_engine/export/include/visionaiflow/export/OnnxExporter.h` 直接 include 上述两个具体训练头文件, 导出层依赖具体模型实现.
- `cpp/apps/hosts/trainer/src/TrainerJobCoordinator.cpp` 只识别 `trainSingleLabel`, 并直接持有 `AsyncClassificationJob`.
- TensorRT Host 和 OpenVINO Host 在各自 `main.cpp` 中硬编码 `classifyOnnx` 和 `detectYolo11Onnx`, 并存在重复请求解析和检测结果转换逻辑.
- 当前没有统一的模型描述, 模型能力, 类别接口, 模型注册表或工厂.
- 当前 `ProjectType` 位于 `project_store`, 包含检测, 分类, 实例分割, 语义分割, 异常检测, 线检测和 OCR 等类型. 模型接口若直接依赖它, 会导致 models 反向依赖数据持久化层.

## 2. 架构结论

### 2.1 继承方案

采用三级关系:

```text
IModelAdapter
  |- IClassificationModelAdapter
  |    `- LinearClassificationAdapter
  `- IDetectionModelAdapter
       `- Yolo11DetectionAdapter
```

结论:

- YOLO11 检测适配器应继承检测类别接口, 检测类别接口再继承通用模型接口.
- 不建议所有具体模型直接继承一个包含全部任务方法的 `BaseModel`.
- 不为尚未实现的模型类别提前创建空基类和空工程. 当第一个真实模型进入某类别时再补充对应类别接口.
- OCR Pipeline 是检测模型和识别模型的组合编排, 不应通过多层模型继承表达.

### 2.2 适配器和神经网络本体必须分开

`Yolo11GridDetectorImpl`, `Yolo11TinyDetectorImpl`, `LinearClassifierImpl` 仍然是 `torch::nn::Module` 的具体实现. 它们不继承 `IModelAdapter`.

适配器负责:

- 稳定模型 ID 和版本.
- 声明支持的项目类型和能力.
- 校验数据集和模型配置.
- 创建网络, loss, metrics, decoder, training session 和 exporter 所需组件.
- 描述输入输出签名和模型包契约.
- 把模型特有实现组合到统一流程.

通用引擎负责:

- 训练生命周期和状态机.
- dataloader, AMP, optimizer 调度, checkpoint 通用存储和恢复.
- ONNX 文件写入, 校验和模型包组装.
- IPC, 进程隔离, GPU lease 和 Host 生命周期.

这样可以避免 `IModelAdapter` 变成同时管理 UI, IPC, 训练循环, 推理后端和模型数学细节的 God Object.

### 2.3 不使用进程内第三方模型 DLL ABI

- 内置模型适配器静态链接到 Trainer Host 和需要它的工具中.
- Registry 只管理编译期已知的适配器工厂.
- 将来如果允许外部模型, 使用独立签名 Host + IPC 协议隔离, 不直接加载任意 LibTorch C++ DLL.
- TensorRT/OpenVINO Host 消费稳定模型包和 decoder contract, 不跨进程传递 C++ adapter 或 `torch::Tensor` 对象.

## 3. 目标目录和 qmake 子工程

仅在对应阶段有真实代码时创建以下目录:

```text
cpp/models/
  models.pro
  api/
    models_api.pro
    include/visionaiflow/models/api/
      IModelAdapter.h
      ModelCapability.h
      ModelDescriptor.h
      ModelRequests.h
      ModelRegistry.h
      ModelSignature.h
    src/
      ModelRegistry.cpp
  classification/
    common/
      classification_common.pro
      include/visionaiflow/models/classification/
        IClassificationModelAdapter.h
        ClassificationContracts.h
    linear/
      linear_classification.pro
      include/visionaiflow/models/classification/linear/
        LinearClassificationAdapter.h
        LinearClassifier.h
      src/
        LinearClassificationAdapter.cpp
        LinearClassifier.cpp
  detection/
    common/
      detection_common.pro
      include/visionaiflow/models/detection/
        IDetectionModelAdapter.h
        DetectionContracts.h
        DetectionPostProcessor.h
      src/
        DetectionPostProcessor.cpp
    yolo11/
      yolo11.pro
      include/visionaiflow/models/detection/yolo11/
        Yolo11DetectionAdapter.h
        Yolo11Detector.h
        Yolo11Loss.h
        Yolo11Metrics.h
        Yolo11Augmentation.h
        Yolo11DetectionDecoder.h
      src/
        Yolo11DetectionAdapter.cpp
        Yolo11Detector.cpp
        Yolo11Loss.cpp
        Yolo11Metrics.cpp
        Yolo11Augmentation.cpp
        Yolo11DetectionDecoder.cpp
```

目标库名称建议:

- `vaf_models_api`
- `vaf_classification_common`
- `vaf_linear_classification`
- `vaf_detection_common`
- `vaf_yolo11`

依赖方向:

```text
core/foundation + core/domain
              |
              v
         models/api
          /       \
         v         v
classification  detection/common
    |                |
    v                v
linear             yolo11
       \            /
        v          v
       ai_engine/training
               |
               v
        ai_engine/export
               |
               v
          apps/hosts
```

禁止反向依赖:

- `models` 不依赖 `apps`, `runtime/ipc`, Qt Widgets 或具体 Host.
- `models/api` 不依赖具体模型, LibTorch 网络实现或具体推理后端.
- `ai_engine/export` 不再 include 具体训练模型头文件.
- `core/domain` 不依赖 `project_store`.
- UI 和 IPC DTO 不直接暴露 `torch::Tensor`, optimizer 或网络对象.

## 4. 通用接口设计

以下为语义要求, 后续任务应根据现有 `foundation::Result<T>` 风格实现, 不应照搬为未经编译验证的占位代码.

### 4.1 `ModelDescriptor`

至少包含:

- `modelId`: 稳定且不可随显示名称变化, 例如 `classification.linear.v1`, `detection.yolo11.grid.v1`.
- `adapterVersion`: 适配器实现版本.
- `displayName`: 仅用于显示.
- `projectTypes`: 支持的项目类型集合.
- `capabilities`: Train, Resume, Evaluate, ExportOnnx, Decode 等能力集合.
- `inputSignature` 和 `outputSignature`.
- `configSchemaVersion`.
- `artifactContractVersion`.
- 支持的 runtime 或 artifact 格式, 但不把 TensorRT/OpenVINO 运行对象放入描述结构.

### 4.2 `IModelAdapter`

通用接口只承担所有模型都具备的最小能力:

- 返回 `ModelDescriptor`.
- 校验通用模型配置.
- 校验数据集描述和项目类型兼容性.
- 创建或描述 `ModelGraph`.
- 创建训练会话, 评估会话和导出提供者时先校验 capability.
- 返回模型包所需的输入输出签名及 artifact contract.

不放入通用接口:

- 检测框, mask, OCR token 等类别专属 DTO.
- 具体 loss 函数参数.
- TensorRT/OpenVINO context.
- UI 控件或 IPC `QCborMap` 解析.

### 4.3 类别接口

`IClassificationModelAdapter` 负责分类专属约束:

- SingleLabel/MultiLabel 支持声明.
- 标签和类别数校验.
- 分类输出解释及 metrics contract.

`IDetectionModelAdapter` 负责检测专属约束:

- 图像输入, class count 和检测标注校验.
- decoder, NMS, letterbox 和坐标恢复 contract.
- 检测 loss/metrics 组件的创建.

类别接口应返回抽象会话或组件, 具体 YOLO11 类型只在 YOLO11 模块内部可见. 如为了渐进迁移暂时保留旧 public header, 旧接口必须真实转发到新实现, 不允许空 wrapper 或假成功.

### 4.4 `ModelRegistry`

要求:

- 使用显式的 `RegisterBuiltInModels(registry)` 组合入口, 不依赖不可控的全局静态注册副作用.
- 注册内容是 `modelId -> factory`.
- 重复 ID 注册返回明确错误.
- 未知 ID 返回明确错误和 `errorMessage`, 禁止自动回退到默认模型.
- 根据项目类型查询时必须过滤不兼容模型.
- Registry 自身不持有训练 job 或 GPU 资源.
- Registry 的单元测试不需要启动 Host 或 GPU.

### 4.5 项目类型归属

推荐把 `ProjectType` 和 `ClassificationMode` 的领域定义移动到 `cpp/core/domain`, 例如 `visionaiflow/domain/ProjectType.h`.

- `project_store::ProjectDefinition` 使用领域枚举, 不再拥有枚举定义.
- 为已有调用者提供真实转发的兼容 alias 或转换函数, 分阶段修改 include.
- `models/api` 只依赖 `core/domain`, 避免 `models -> data/project_store` 的错误依赖.
- 完成调用者迁移和测试后再移除兼容层.

## 5. 分阶段执行计划

每一阶段都要单独编译, 测试和提交. 不要在一个提交中同时做大规模文件移动, 接口语义修改和 Host 协议修改.

### 阶段 0: 冻结目录迁移基线

任务:

1. 让用户确认唯一工具链是 Qt 6.9.2/MSVC2022 还是仓库当前 Qt 6.7.3/MSVC2019.
2. 同步 `AGENTS.md`, `.qmake.cache`, `qmake/common.pri`, `qmake/cuda.pri`, `tools/build-qmake-*.cmd` 和 trainer wrapper.
3. 删除旧 Makefile 或构建目录属于清理动作. 执行前按仓库规则请求用户确认, 然后重新运行 qmake, 不复用混合工具链产物.
4. 完成 Release 全量构建, 确认 app, cli, 三个 Host, 所有静态库和 15 个测试目标都是本次构建的新产物.
5. 运行 15 个现有测试并保存逐项退出码和日志.
6. 复核 `git status`, 暂存迁移, 使用 `git diff --cached --summary` 确认 Git 识别大部分文件为 rename.
7. 处理 `cpp/tests/CMakeLists.txt`, `重划目录.txt` 和空目录残留. 任何批量删除先请求用户确认.
8. 将目录迁移作为独立提交, 接口重构从干净工作树开始.

验收:

- 工作树干净.
- 单一工具链无混用.
- Release 全量编译完成.
- 15 个原测试全部通过.
- 迁移提交不包含模型接口语义变化.

### 阶段 1: 建立领域类型和最小 `models/api`

任务:

1. 将项目类型的领域定义下沉到 `core/domain`, 保持 project_store 兼容入口.
2. 新建 `cpp/models/api/models_api.pro`.
3. 实现 `ModelCapability`, `ModelDescriptor`, `ModelSignature`, 请求上下文和 `IModelAdapter`.
4. DTO 使用 QtCore 或标准库值类型, 不依赖 Qt Widgets, IPC 和 LibTorch.
5. 在 `cpp/models/models.pro` 中加入 `models_api`, 并为后续模块声明准确 depends.
6. 先只为当前已有模型表达能力, 不预填尚未实现模型的假 descriptor.

测试:

- descriptor 字段完整性和稳定 ID 校验.
- capability 查询.
- project type/config schema 不兼容错误.
- 空 ID, 空版本, 无输入输出签名等失败路径.

提交建议: `refactor(models): add core model adapter contracts`

### 阶段 2: 实现显式模型注册表

任务:

1. 实现 `ModelRegistry` 和 factory contract.
2. 实现 `RegisterBuiltInModels` 组合入口, 初期可以只注册尚未迁移的兼容 adapter, 但必须转发真实实现.
3. Registry 只创建 adapter 或 operation session, 不直接解析 IPC.
4. 为 app/trainer host 提供按 `modelId` 和 `ProjectType` 查询的稳定入口.

测试:

- 正常注册和创建.
- 重复 ID.
- 未知 ID.
- modelId 与 ProjectType 不兼容.
- factory 返回失败时错误不被吞掉.

提交建议: `refactor(models): add explicit model registry`

### 阶段 3: 先完成分类模型纵向迁移

选择分类作为第一个纵向切片, 因为当前 `TrainerJobCoordinator` 只真正调度分类训练, 影响面小于先改 YOLO11 全链路.

任务:

1. 新建 `classification/common` 和 `classification/linear` 真实模块.
2. 将 `LinearClassifierImpl` 和模型专属创建逻辑从 `ai_engine/training` 移入 `models/classification/linear`.
3. 将通用 checkpoint 状态, AMP, dataloader 和训练生命周期留在 `ai_engine/training`.
4. 实现 `LinearClassificationAdapter final : IClassificationModelAdapter`.
5. 将 `AsyncClassificationJob` 改为依赖分类 adapter/session, 不再创建固定的 Linear 模型.
6. 将 `TrainerJobCoordinator` 从直接持有 `AsyncClassificationJob` 改为持有通用训练 job/session 接口.
7. IPC 暂时兼容 `trainSingleLabel`, 内部映射到 `modelId=classification.linear.v1`. 映射必须显式记录, 未知 modelId 必须失败.
8. 旧 `LinearClassifierTrainer.h` 如暂时保留, 只作为兼容 wrapper 并转发到真实新实现.

测试:

- 现有 `tst_LinearClassifierTrainer.cpp` 全部保留并迁移路径.
- adapter 创建, 配置校验, single-label, multi-label.
- train/evaluate, checkpoint save/load/resume.
- Trainer Host 通过 modelId 调度.
- 缺失数据集, 类别数错误, checkpoint 不兼容和运行时失败均返回明确错误.

提交建议: `refactor(models): migrate linear classification through adapter`

### 阶段 4: 迁移 YOLO11 检测模型

任务:

1. 在 `models/detection/common` 中放置检测 DTO, 类别接口及通用后处理.
2. 将库名从含糊的 `vaf_models_common` 调整为 `vaf_detection_common`, 同一提交更新所有链接者.
3. 将 `Yolo11TinyDetectorImpl`, `Yolo11GridDetectorImpl` 移到 `models/detection/yolo11`.
4. 将 YOLO11 特有 assignment, loss, metrics 和 augmentation 从通用 training 模块移到 YOLO11 模块.
5. 通用训练循环, checkpoint 基础设施和设备管理继续留在 `ai_engine/training`.
6. 实现 `Yolo11DetectionAdapter final : IDetectionModelAdapter`.
7. 保留 Tiny/Grid 为同一 adapter 下的显式 variant, 或使用两个稳定 modelId. 不得根据输入形状静默猜测 variant.
8. decoder 使用统一 Detection contract, 并完整处理 letterbox, ROI, 原图坐标恢复, 空检测和无效输出错误.

测试:

- 原有 829 行 YOLO11 测试覆盖不得减少.
- Tiny/Grid 创建和 parameter names.
- assignment, loss, train/evaluate, metrics, augmentation.
- decoder, NMS, letterbox 和坐标转换.
- checkpoint save/load/resume.
- 未支持 variant, 空输出, 非法 class count 和无有效框的错误路径.

提交建议: `refactor(models): migrate yolo11 detection through category adapter`

### 阶段 5: 解耦导出和模型包

任务:

1. 移除 `OnnxExporter.h` 对 `LinearClassifierTrainer.h` 和 `Yolo11DetectionTraining.h` 的直接 include.
2. 定义通用 export provider 或 export session, 由 adapter 提供模型图, 参数映射, example input 和签名.
3. ONNX writer 只依赖通用导出 contract 和 ModelGraph.
4. 将 `ClassificationPackageMetadata` 和 `Yolo11DetectionPackageMetadata` 中重复字段提取到公共模型包元数据, 类别特有字段放入签名或扩展区.
5. 保持既有 opset 12, IR 9, allowed ops, SHA256, package version 和签名验证行为不变.
6. package 内必须写入 `modelId`, `adapterVersion`, `projectType`, `input/output signature`, `decoderId`, `artifactContractVersion`.
7. 旧导出函数暂时保留时必须真实转发, 不允许生成空 ONNX 或硬编码成功.

测试:

- 分类和 YOLO11 的既有 ONNX 导出测试.
- graph signature 和 parameter mapping.
- package manifest 兼容性.
- hash, 缺失文件, 非法 opset, 不支持算子和签名失败.
- 旧模型包读取兼容测试, 并明确最低支持版本.

提交建议: `refactor(export): export models through adapter contracts`

### 阶段 6: 统一推理契约, 保持后端隔离

任务:

1. 在有真实代码落入时启用 `cpp/inference/api`, `preprocessing`, `postprocessing`, `tensorrt`, `openvino` 目录. 不先创建空库.
2. 定义后端中立的 `IInferenceBackend` 或 inference session contract, 只表达加载 artifact, 验证签名和执行请求.
3. decoder registry 与 runtime backend 分开. 后端输出标准 tensor/result, decoder 根据 `decoderId` 生成类别结果.
4. 抽取 TensorRT/OpenVINO Host 中重复的请求字段校验, 图像输入 contract 和检测结果序列化.
5. Host `main.cpp` 只保留启动, IPC 命令路由和生命周期管理.
6. 保持 TensorRT 和 OpenVINO 为独立进程. 不把 backend 对象放入模型 adapter.
7. 兼容 `classifyOnnx` 和 `detectYolo11Onnx`, 新协议优先使用模型包中的 modelId/decoderId.

测试:

- 同一模型包在支持的两个后端上获得结构一致的输出.
- 预处理和后处理 parity.
- modelId, decoderId, shape, dtype, package version 不匹配.
- Runtime DLL 缺失, engine 构建失败, 模型加载失败和空输出必须记录日志并返回明确 `errorMessage`.

提交建议: `refactor(inference): dispatch model packages through backend contracts`

### 阶段 7: 应用层和 IPC 接入

任务:

1. 应用层按 `ProjectType` 从 Registry 查询可用模型, 存储稳定 modelId, 不存 C++ 类型名.
2. 训练请求增加 `modelId`, `adapterVersion` 和配置 schema version.
3. 推理请求优先从已验证模型包读取 contract, 不让 UI 重复决定 decoder 细节.
4. 为旧 operation 保留版本化兼容映射, 明确废弃周期.
5. UI 仅在确实需要模型选择和配置时修改. 如新增 UI, 必须创建或修改 `.ui` 文件, 控件和基础布局优先放在 `.ui` 中.

测试:

- project type 到模型列表的过滤.
- 保存和重新打开项目后 modelId 不变.
- IPC 新旧协议兼容.
- 不支持模型, 版本不兼容和模型包缺失的用户可见错误.

提交建议: `refactor(app): select and dispatch models by stable id`

### 阶段 8: 收紧依赖和清理兼容层

任务:

1. 使用 `rg` 确认旧训练头, 旧命名空间和旧导出函数无调用者.
2. 再删除兼容 wrapper. 删除文件前遵守用户确认规则.
3. 将 `qmake/common.pri` 的全局 include 列表缩减为真正全局的基础配置, 各 `.pro` 自己声明 include 和 link 依赖.
4. 检查 `.pro` 的 `PRE_TARGETDEPS`, `LIBS`, `depends` 与架构依赖图一致.
5. Release 输出仍统一进入维护用 bin/lib 目录. 发布包组装使用独立 deploy/package 步骤, 不在每个模型 `.pro` 中重复复制 DLL.
6. 更新执行规范, 架构说明, 模型接入指南和错误码说明.

验收:

- 不存在 `ai_engine/training` 直接定义具体模型网络的情况.
- 不存在 exporter 直接 include 具体模型训练头的情况.
- Host 不再以 if/else 硬编码每个具体模型实现.
- 模块外无法依靠全局 include 路径绕开正式依赖.

## 6. 测试和验证矩阵

| 层级 | 必须覆盖 |
| --- | --- |
| models/api unit | descriptor, capability, registry, duplicate ID, unknown ID, project type mismatch, factory failure |
| classification unit | config validation, single/multi label, train/evaluate, checkpoint/resume, export provider |
| detection unit | target assignment, loss, metrics, augmentation, decode, NMS, letterbox, coordinate conversion |
| export unit | graph/signature, opset/IR, parameter mapping, package manifest, hash/signature errors |
| trainer integration | modelId dispatch, cancel, resume, GPU lease release, invalid config and runtime error propagation |
| inference integration | TensorRT/OpenVINO contract parity, decoder selection, package/runtime failure |
| compatibility | `trainSingleLabel`, `classifyOnnx`, `detectYolo11Onnx` 旧协议真实转发 |
| Release regression | 当前 15 个测试全部通过, app/cli/三个 Host 均能启动并报告真实依赖错误 |

执行测试时必须记录:

- 测试目标名称.
- 本次生成的 exe 时间戳.
- 退出码.
- 通过/失败数量.
- 失败日志路径.
- CUDA/TensorRT/OpenVINO 测试是否因环境条件跳过. 跳过必须有明确理由, 不能当作通过.

## 7. 兼容和错误处理原则

- 所有旧 wrapper 必须转发到统一真实接口.
- 不允许未知 modelId 回退到 Linear 或 YOLO11.
- 不允许模型初始化失败后返回空结果或成功状态.
- 文件缺失, DLL 缺失, Runtime 异常, 空 tensor, 空 embedding, 空 mask, 无有效轮廓和不兼容 artifact 均返回明确 `errorMessage` 并记录结构化日志.
- checkpoint 和模型包必须校验 modelId, adapterVersion, schema version, 输入输出签名和参数映射.
- 坐标转换, mask/ROI/轮廓转换必须位于可测试的类别组件中, 不散落在 Host `main.cpp`.
- 不在接口重构阶段顺便修改模型算法数值行为. 如果确需修改, 另建独立提交并增加 parity 测试.
- `.cpp` 和 `.h` 保持 GBK/System 编码. Markdown, JSON, JSONL, JSON Schema 和 CMake 保持 UTF-8.

## 8. 每个新模型的接入清单

后续新增模型时只需要:

1. 选择已有类别接口. 只有没有匹配类别时才新增类别接口.
2. 实现具体 adapter 和 `torch::nn::Module` 网络, 两者使用组合关系.
3. 提供稳定 modelId, adapter version, capability 和 project type.
4. 实现配置/数据集校验, 输入输出签名和 artifact contract.
5. 提供训练, 评估, 导出, decoder 等声明支持的真实能力.
6. 在 `RegisterBuiltInModels` 中显式注册.
7. 增加 unit, integration, package 和错误路径测试.
8. 更新对应 `.pro` 和依赖, 不修改无关模块的全局 include.

## 9. 提交和回滚策略

- 保留阶段 0 的纯目录迁移提交作为可回滚基线.
- 阶段 1 至阶段 8 每阶段至少一个独立提交.
- 文件移动尽量先执行并提交, 再改内容, 便于 Git 识别 rename 和代码审查.
- 每个阶段失败时只回滚该阶段, 不回退已经验收的目录迁移.
- 不使用 `git reset --hard`, `git checkout --` 或批量删除命令覆盖用户工作区.
- 提交前检查 `git status`, `git diff --check`, Release 编译和对应测试.

## 10. 最终完成标准

同时满足以下条件才算模型接口重构完成:

- 工具链只有一个权威配置, Release 构建无 VS2019/VS2022 混用.
- 目录迁移和接口重构提交历史清晰分离.
- `IModelAdapter`, 分类接口, 检测接口和 Registry 均有真实实现和测试.
- `LinearClassificationAdapter` 和 `Yolo11DetectionAdapter` 通过 Registry 创建.
- 具体 LibTorch 网络只继承 `torch::nn::Module`, 不继承模型 adapter.
- Trainer Host 根据 modelId 调度, 不直接绑定 `AsyncClassificationJob`.
- exporter 不直接依赖具体训练头.
- TensorRT/OpenVINO Host 使用统一模型包和 decoder contract, 同时保持进程隔离.
- 所有失败路径都有明确 `errorMessage` 和日志, 不存在假实现或静默回退.
- 当前 15 个测试及新增模型接口测试全部在统一 Release 工具链下通过.
- 发布 bin 由统一输出和部署流程生成, 不依赖各模型工程散落的 post-build copy.

## 11. 后续 Codex 任务的第一步

不要立即写模型接口代码. 先向用户报告以下阻塞项并取得工具链选择:

1. 当前任务约定 Qt 6.9.2/MSVC2022, 仓库却改为 Qt 6.7.3/MSVC2019.
2. 2026-08-04 的 Release 构建没有完成, 现有测试 exe 是旧产物.
3. 目录迁移仍未暂存和提交.

只有用户确认工具链, 并完成阶段 0 的 Release 基线后, 才从阶段 1 开始实现.
